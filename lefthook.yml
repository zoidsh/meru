pre-commit:
  commands:
    fmt:
      run: bun fmt --no-error-on-unmatched-pattern {staged_files}
      stage_fixed: true
    lint:
      run: bun lint:fix --no-error-on-unmatched-pattern {staged_files}
      stage_fixed: true
