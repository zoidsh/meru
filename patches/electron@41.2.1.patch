diff --git a/electron.d.ts b/electron.d.ts
index f806d6766269d80b14a64db724b8e6cf42d9e9e5..bd5a7b314720a4aec52eb5886cf4cc87b79d0271 100644
--- a/electron.d.ts
+++ b/electron.d.ts
@@ -18226,8 +18226,10 @@ declare namespace Electron {
      * A `WebContents` property containing a reference to the displayed `WebContents`.
      * Use this to interact with the `WebContents`, for instance to load a URL.
      *
+     * Becomes `undefined` after the underlying `WebContents` is destroyed (e.g.
+     * from inside a `destroyed` listener, or after the owning window closes).
      */
-    readonly webContents: WebContents;
+    readonly webContents: WebContents | undefined;
   }
 
   interface WebFrame {
